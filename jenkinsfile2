pipeline {
    agent {
        docker {
            image 'ubuntu'
            label 'my-defined-label'
        }
        stages {
            stage('One') {
            echo 'hello'
            }
            }
    }
}
